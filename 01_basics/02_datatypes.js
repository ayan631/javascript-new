"use strict";       //to treats all js code as newer version.
// alert(3+3)       //alert is use for browser not in node.js
console.log(3*4)    //12

// let name = "Anupam" //name is a reserved word don't use as a variable name 
let uname="Ayan"            //type=>String
let age = 26                //type=>number
let avg = 79.78             //type=>number
let temperature = null      //type=>object it is a standalone value
let isLoggedin = false      //type=>boolean
let state                   //undefined

/*
In javascript, type of null is object, but type of undefined is undefined itself
*/

console.log(typeof isLoggedin)